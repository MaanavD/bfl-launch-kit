{
  "name": "bfl-thumbnail-pipeline",
  "version": "1.0.0",
  "description": "Automated YouTube thumbnail pipeline — generates and sets thumbnails on video publish",
  "type": "module",
  "scripts": {
    "auth": "node src/auth.js",
    "start": "node src/server.js",
    "trigger": "node src/trigger.js"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "express": "^4.21.0",
    "googleapis": "^171.4.0",
    "xml2js": "^0.6.2"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
